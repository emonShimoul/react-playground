import React from 'react';

const Dashboard = ({title}) => {
    return (
        <div>
            <h2>{title}</h2>
            <p>This is the dashboard</p>
        </div>
    );
};

export default Dashboard;